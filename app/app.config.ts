export default defineAppConfig({
  ui: {
    colors: {
      primary: "lime",
      neutral: "neutral",
    },
    card: {
      slots: {
        root: "rounded-xl overflow-hidden",
        body: "p-0 sm:p-0",
      },
    },
  },
});
